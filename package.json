{
  "name": "zenlink-interface",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@7.13.5",
  "license": "MIT",
  "repository": "https://github.com/zenlinkpro/zenlink-interface.git",
  "engines": {
    "node": ">=16.x",
    "pnpm": ">=7.x"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "generate": "turbo run generate --filter=[HEAD^1]",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rm -rf node_modules",
    "test": "turbo run test --parallel --filter=[HEAD^1]"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^0.27.0",
    "eslint": "^8.25.0",
    "pnpm": "^7.13.5",
    "turbo": "latest"
  }
}
